dbt build ->in actual production full pipeline check ,superset of(run,test,snapshots,seeds)
runs in correct DAG order what if run build and then testing failed for dq its bad,
so dbt build runs in crct order saving us in prod. if any failure it halts next phase
dbt run  ->for testing will matrialize
dbt test

--select "modelname"

0.modelname
1.+modelname upstream
2.modelname+ downstream
3.+modelname+ up/down

0. dbt core and now dbt fusion new rust faster code
1.profiles.yml file here setted up using config, usually we do it by mentioning connetions to platform and targt schema
2.dbT_projects.yml for setting up materiailizing at folder level. name , model should be same
3.source yml for setting up sources e.g {{ source('jaffle_shop','customers') }} , it can be generated by codgen using below code
{{ codegen.generate_source(schema_name= 'jaffle_shop', database_name= 'raw') }}
and also add freshness-> dbt source freshness command , click generate model to develop code. by usin source linage also works crctly
4.packages.yml for installing any packages ->dbt deps command
5. code level normal cte , join tab2 using(col1), use __ and call jinja'schema
6. dbt hub.dbt  and get packages and its documentations

-------------------------------------------------------------------
Tests
1.generic tests the defaults
unique,not null, accepted values,relationship
 - name: stg_jaffle_shop__orders (model)
      columns: 
          - name: status (column)
            tests: 
              - accepted_values:
              -relationship:
                field:cust_id
                to: ref('another model)

2.singluar which we build
refer dbt utils, dbt expection packages for many prebuild templates
create inside tests folder

schema.yml for adding test cases using models ->dbt test
dbt test --select "mention model name"

we can apply tests on sources aswell similar to models but in same sources.yml file
nd run for source alone dbt test --select source:jaffle_shop
dbt test runs all tests in the dbt project
dbt test --select test_type:generic
dbt test --select test_type:singular
dbt test --select one_specific_model

-------------------------------
use dbt copilot for test case generations
dbt seeds load csv files to warehouse
dbt snapshots do cdc .

--------------------------------
documentations

--use descriptions at model or column level in src.yml for sources or schema yml for models
create doc blocks using md files {% docs doc_block_name %} djmscsjh {%enddocs%} use preview to check
and mention in src.yml/schema "{{doc('doc_block_name')}}"
we can have multiple doc blocks in same md file so name is must. we can view this in our dbtcatalog


-----------orchestration
1.create new env , 2 env types ->development,deployment

2.deployment type->general,stg,prod

3.branch to run can be mentioned.if not defaults
4.connection using  profiles

5.create job , many types choose deloyjob
give a name,mention what command to run dbt build or any specific model.
set freshness,generate runs.
set intervel,cron etc
another option set when other job completes run this like that.
